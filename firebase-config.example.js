// Copy this file to firebase-config.js and fill in your real Firebase project config.
// DO NOT commit your real config if this repo will be public until you are comfortable exposing it.
// Firebase web config keys are not secrets, but treat them with care.

window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "xxxxxxxxxxxx",
  appId: "1:xxxxxxxxxxxx:web:xxxxxxxxxxxxxxxxxxxx",
  // optional:
  // measurementId: "G-XXXXXXXXXX"
};

// Optional: simple feature flag (leave true once configured)
window.FIREBASE_ENABLED = true;